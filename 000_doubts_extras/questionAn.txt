inline: inline element are those which dosent occupy the whole line.
block : which does occupy the whole line
inline-block: same as inline but respects height and width(until and unless specified).
float: uses of float
fontBoosting : automatic increase in size of fonts. 
different ype of units of size(also care of font size).